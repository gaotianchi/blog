server:
  port: 8080

spring:
  application:
    name: userservice
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://127.0.0.1:9000/oauth2/jwks
  datasource:
    url: jdbc:mysql://localhost:3306/blog_user?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: gaotianchi
    password: "{noop}password"
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org.springframework.security: debug
